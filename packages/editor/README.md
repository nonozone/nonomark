# @nonoim/editor

Compatibility package for existing Vue projects. New Vue projects should install `@nonoim/editor-vue`.

The compatibility package also forwards `@nonoim/editor/lazy` to the Vue lazy entry. New code should import `NonoLazyEditor` from `@nonoim/editor-vue/lazy` directly.
